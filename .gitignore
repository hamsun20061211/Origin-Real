# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# bulk-collected training images (local only)
/data/raw/

# SD + rembg + Real-ESRGAN 파이프라인 출력 (용량 큼)
/data/train/

# synced copies for training (large)
/data/processed/images/*
!/data/processed/images/.gitkeep

# TripoSR FastAPI temp GLB downloads
/triposr-server/temp_glb/

# Photogrammetry job workspace (ZIP extracts + COLMAP outputs)
/photogrammetry-server/data/

# Real-ESRGAN checkpoints (auto-download)
/triposr-server/weights/*.pth

# Local venv for TripoSR + engine (see triposr-server/start-engine.ps1)
/.tripo-venv/

# LoRA weights (large); keep adapter_config.json locally if needed
/models/checkpoints/*.safetensors

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (includes .env.local — never commit API keys)
.env*.local
.env.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts
